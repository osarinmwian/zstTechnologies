import { StyleSheet, Text, View } from "react-native";
import React from "react";

const ZstTaskItems = () => {
  return (
    <View>
      <Text> ShoppingListScreen</Text>
    </View>
  );
};

export default ZstTaskItems;

const styles = StyleSheet.create({});
